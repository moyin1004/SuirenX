# 下一阶段实现进度

本文件用于在长任务或上下文刷新后恢复工作。计划来源：[next-phase-plan.md](next-phase-plan.md)。

## 当前检查点

- M1–M4 的实现、回归测试和模拟器验收已完成；M5 已完成服务端账号认证、owner 隔离、版本化资产/用品同步、tombstone、幂等和双游标增量接口，以及 Android 登录、按服务器/账号隔离的 Room cache/outbox、资产与用品冲突处理、最近同步时间标记和显式本地迁入向导。
- 本地模式使用 Room 作为唯一事实来源；远程模式使用已配置服务器；两者不自动合并、上传或回退。
- 本地 JSON 备份覆盖资产、用品、归档、状态、标签和时间字段，恢复前自动备份并在同一 Room 事务中替换。
- 最后验证：Go `go test -count=1 ./...`（含两个并发编辑器冲突和 SQLite 文件复制恢复测试）、资产与 M5 Protobuf descriptor、Android 全量 `testDebugUnitTest`/`core:model:test`/`app:assembleDebug`、本地真实 HTTP 注册+同步烟测、最终 APK 安装启动、`git diff --check` 和 `gofmt` 均通过；此前模拟器验证工具入口、模式隔离、设置页、本地空资产态、创建资产后进程重启仍可读取、200% 字体语义树和临时宽屏显示。跨账号 UUID 碰撞的空远程快照也已做不透明冲突处理，保留服务器时不会解析空日期或泄漏远程数据。
- 设计文档：[local-mode.md](local-mode.md)、[m5-sync-design.md](m5-sync-design.md)。

## 里程碑

- [x] M1：本地模式、模式切换、本地资产仓储、备份与恢复
- [x] M2：资产购买渠道、保修截止日、备注、标签
- [x] M3：工具页与用品保质期管理（系统通知保留为可选增强，应用内强提示完整可用）
- [x] M4：主题、响应式布局与无障碍验收
- [x] M5：身份、跨设备同步与远程离线能力设计/实现（服务端、Android cache/outbox、资产/用品冲突处理、显式迁入和备份恢复自动演练已完成）

## 恢复方式

1. 先运行 `git status --short` 和 `git diff --check`。
2. 阅读本文件最后一个检查点及 `docs/TODO.md` 的 P1 复选框。
3. 运行对应模块测试，再继续未完成里程碑；不要重做已勾选项目。

## M5 检查结果

- 收口检查点（2026-09-06）：固定 `hz v0.9.7` 生成脚本和资产/M5 descriptor 校验通过；最新 APK 已重新安装到 `medium_phone` 模拟器并启动，未发现 `FATAL EXCEPTION`。当前工作区保留全部实现与文档变更，后续刷新可从本文件继续核对，不需要重做 M1–M5。

- Android 资产和用品页面都有待同步数量、最近成功同步时间、冲突快照以及“保留本机/保留服务器”入口；冲突记录与 outbox 使用独立 Room 表，重启后仍可继续处理。
- 设置页的迁入流程只在用户点击后运行：预览稳定 ID/名称+金额+购买日期（用品为 ID/名称+包装到期日）去重，先生成安全备份，再逐条以稳定 ID 和幂等 key 入队/上传；中断后重复执行会跳过已存在记录，本地数据不被修改。
- 远程用品提醒使用服务器缓存，联网访问成功后记录 `lastSyncedAt`，断网时仅显示该账号/服务器的最近缓存并明确标注来源；账号切换由 cache key 隔离。
- Go 集成测试覆盖两台并发编辑器的版本冲突、重复请求、tombstone、账号隔离以及复制 SQLite 文件后重新打开并拉取数据的备份恢复演练。
